
/**
 * @brief This stores flags for the visible faces of a given block
 * 
 */
struct Block
{
    bool x0; // -x
    bool x1; // +x
    bool y0; // -y
    bool y1; // +y
    bool z0; // -z
    bool z1; // +z
};